version: 1
frontend:
  phases:
    build:
      commands:
        - npm install
        - npm run build
  artifacts:
    baseDirectory: gym-buddy-frontend/build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
